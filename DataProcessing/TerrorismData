import pandas as pd


def read_csv(filename, encoding='ISO-8859-1'):
    df = pd.read_csv(filename, encoding=encoding)
    return df


def select_columns(dataframe, columns):
    return dataframe[[columns]].copy()